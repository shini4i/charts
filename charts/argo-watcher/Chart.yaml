apiVersion: v2
name: argo-watcher
description: A Helm chart for deploying argo-watcher
type: application
sources:
- https://github.com/shini4i/argo-watcher
- https://github.com/shini4i/charts/tree/main/charts/argo-watcher
version: 0.8.1
keywords:
- argocd
- gitops
- automation
kubeVersion: '>=1.21.0-0'
appVersion: v0.9.1
maintainers:
- name: Vadim Gedz
  email: github@shini4i.dev
  url: https://github.com/shini4i
icon: https://argo-cd.readthedocs.io/en/stable/assets/logo.png
annotations:
  artifacthub.io/signKey: |
    fingerprint: FF1E9948F6234DC6D70AB47BF509F29B63C1DC2B
    url: https://shini4i.github.io/charts/pgp_keys.asc
  artifacthub.io/changes: |
    - kind: changed
      description: Migrated from Deployment to StatefulSet with volumeClaimTemplates
    - kind: changed
      description: Simplified migration Job to use main image with --migrate flag
    - kind: changed
      description: Moved updater config from argo.updater to top-level updater key
    - kind: changed
      description: Split healthChecks into separate livenessProbe, readinessProbe, and startupProbe configs
    - kind: changed
      description: Renamed container from generic 'app' to chart name
    - kind: changed
      description: Replaced default scaffold NOTES.txt with argo-watcher-specific output
    - kind: added
      description: Added valueFrom support for extraEnvs
    - kind: added
      description: Added values.schema.json for input validation
    - kind: added
      description: Added PodDisruptionBudget support
    - kind: added
      description: Added topologySpreadConstraints support
    - kind: added
      description: Added automountServiceAccountToken control on ServiceAccount
    - kind: added
      description: Added terminationGracePeriodSeconds support
    - kind: fixed
      description: Fixed empty secretRef rendering when argo.secretName is not set
    - kind: fixed
      description: Fixed storageClassName field mismatch in volumeClaimTemplates
    - kind: fixed
      description: ssh-known-hosts ConfigMap now only created when updater is enabled
    - kind: fixed
      description: Network policies scoped to release pods instead of entire namespace
    - kind: fixed
      description: Network policy names prefixed with release name to avoid conflicts
    - kind: fixed
      description: Added serviceAccountName and imagePullSecrets to migration Job
    - kind: fixed
      description: PodMonitor now includes standard chart labels
    - kind: fixed
      description: Pinned busybox image version in test-connection pod
    - kind: fixed
      description: Added securityContext to test-connection pod for restricted cluster compatibility
    - kind: fixed
      description: Fixed inconsistent YAML indentation in values.yaml
    - kind: fixed
      description: Added tests/ to .helmignore to exclude unit tests from packaged chart
